---
"sladocs": minor
---

Add a frontmatter metadata table. Configure `frontmatter.fields` in your config to render selected frontmatter values (with optional `badge` style) between the page title and body. Also adds a top-level `exclude` that merges shared glob patterns into every project.
